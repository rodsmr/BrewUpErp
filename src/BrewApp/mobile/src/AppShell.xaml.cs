namespace BrewApp.Mobile;

public partial class AppShell : Shell
{
    public AppShell()
    {
        InitializeComponent();

        // Register routes for navigation (additional detail pages can be registered here)
        // Routing.RegisterRoute("orderdetail", typeof(Features.Orders.OrderDetailPage));
    }
}
